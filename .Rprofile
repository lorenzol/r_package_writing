.First <- function(){
  # this code runs when the RStudio project is loaded. 
  # It makes all using devtools very easy.
  if (require(devtools)){
    pkg <- as.package(".")
    cat("Use 'load_all()' to load ',pkg$package,')
  }
}